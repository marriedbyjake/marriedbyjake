---
// Fundations
import BaseLayout from "@/layouts/BaseLayout.astro";
import Text from "@/components/fundations/elements/Text.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
---

<BaseLayout title="Contact Married By Jake - Wedding Celebrant">
  <section>
    <Wrapper variant="standard" class="pb-12 pt-48">
      <div class="overflow-hidden grid grid-cols-1 lg:grid-cols-2">
        <div class="p-8 h-full flex flex-col justify-start bg-base-50">
          <div>
            <Text
              tag="h1"
              variant="displayMD"
              class="text-accent-800 font-display text-balance lg:col-span-2"
            >
              Contact Jake
            </Text>
            <!--<div class="space-y-1 mt-8">
              {
                contactInfo.map((info) => (
                  <div class="flex items-start space-x-3">
                    <Text
                      tag="p"
                      variant="textSM"
                      class="font-medium text-accent-800"
                    >
                      {info.label}
                    </Text>
                    {info.href !== "#" ? (
                      <a
                        href={info.href}
                        class="text-secondary-600 hover:text-accent-800 transition-colors"
                      >
                        <Text tag="p" variant="textSM">
                          {info.value}
                        </Text>
                      </a>
                    ) : (
                      <Text tag="p" variant="textSM" class="text-base-600">
                        {info.value
                          .split("\n")
                          .map((line) => line.trim())
                          .join(", ")}
                      </Text>
                    )}
                  </div>
                ))
              }
            </div> -->
          </div>
          
        </div>
        <iframe allowfullscreen="" src="https://app.studioninja.co/contactform/parser/0a800fc9-6fb5-18c4-816f-b6071676039b/0a800fc9-7ac4-1365-817a-e124acbe7fe7?wmode=opaque" data-embed="true" style="min-width: 100%; max-width: 1440px; border: 0px; overflow: hidden; height: 1888px;" id="sn-form-mahsi" height="1488" scrolling="no" title="Contact form for Married By Jake">
</iframe>
      </div>
    </Wrapper>
  </section>
</BaseLayout>
